# Environment Configuration for QA Automation

# Base URL - Target website to test
LOT_BASE_URL=https://leagueoftraders.io

# Authentication Credentials
LOT_EMAIL=your-test-email@example.com
LOT_PASSWORD=your-test-password

# Optional: Enable headless mode (default: true)
HEADLESS=true

# Optional: Enable video recording (default: only on failure)
VIDEO=on-first-retry
